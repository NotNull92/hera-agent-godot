# Claude Code handoff — Phase 8 agent harness kits (2026-07-13, from Codex)

Phase 8's agent harness-kit item is complete. The external winget-pkgs
submission remains deliberately untouched because it needs explicit user
approval.

## Completed

- Added a repository-local Claude Code marketplace at
  `.claude-plugin/marketplace.json` and the `hera-godot` plugin at
  `integrations/claude-code/hera-godot/`.
  - Its `live-editor` skill is auto-invoked for live Godot editor/runtime work
    and is directly available as `/hera-godot:live-editor`.
  - The plugin manifest intentionally has no fixed `version`: Claude Code uses
    the source commit SHA, so active-development installs receive future
    updates without a separate version bump.
- Added the Agent Requested Cursor template at
  `integrations/cursor/hera-godot.mdc` and the copy-paste agent instruction
  document at `integrations/AGENTS.md`.
- Documented all three installation paths in `README.md` and `README.ko.md`,
  and checked off the roadmap item in `docs/ROADMAP.md`.

Each agent-facing instruction document is below the stated ~1k-token budget:
Claude Code skill ~592, Cursor rule ~417, and AGENTS snippet ~388 tokens using
the project's `chars / 4` approximation.

## Verification performed

- `claude plugin validate .` and
  `claude plugin validate .\integrations\claude-code\hera-godot` passed.
  Both print only the expected non-strict warning that the plugin intentionally
  omits a fixed semver version. `--strict` treats that warning as an error.
- JSON manifest parsing, Cursor frontmatter shape, internal integration paths,
  whitespace, and `git diff --check` passed.
- `go build ./...`, `go vet ./...`, and
  `go test -shuffle=on -count=1 ./...` passed.
- `go test -race` was not run; the prior handoff records that this PC's
  antivirus blocks race-instrumented binaries.

The matching Claude Code prompt test was attempted with a transient
`--plugin-dir` and all tools disabled, but this PC is not logged in
(`Not logged in · Please run /login`). No marketplace was installed and no user
settings were changed. Cursor is not installed on this PC, so its actual IDE
load is still unverified.

## Next work

1. Do not submit the existing winget manifest until the user explicitly
   approves a public `microsoft/winget-pkgs` PR.
2. Continue with the Phase 8 headless/CI recipe, then the thin `hera mcp`
   bridge, preserving the low-token CLI-first design.
3. Any v0.9.0 tag, release, Asset Store upload, marketplace publication, or
   third-party PR still needs explicit user approval.
