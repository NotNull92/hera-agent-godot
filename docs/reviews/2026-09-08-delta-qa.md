# Targeted resource validation QA — 2026-09-08

Verdict: **PASS within the delta scope**. Fresh independent execution found no defect in the four requested raw-value cases. No source edits or user-editor access occurred.

Base HEAD independently confirmed: `d6543c421e9b830301a8e0329c7ba56eaa2a1fd8`. Coordinator-supplied source aggregate: `23b9ba74f80ea0571e743015e46d4a1cfbff070404d85eae42b50a8dbae3e3ad` (not independently recomputed). Independently captured file SHA256 values:

- `addons/hera_agent_godot/tools/resource_value_codec.gd`: `c774d046766afc7c10cf8a0dede812534e276f45fe3993cce4e0b3fe09e3379f`.
- `tests/headless/resource_mutation_test.gd`: `91ccfced3ecc6fdabc7dee03331c8f8235bc0f7a635d482945b26078894932ed`.

## Fresh execution

Copied the current addon and current `resource_mutation_test.gd` into a new private minimal project. HOME, USERPROFILE, APPDATA and XDG directories were isolated, and the token was empty. Used the direct executable documented in `docs/DEV_MACHINE.md`:

```text
C:/Users/PC/Downloads/Godot_v4.7-stable_win64.exe/Godot_v4.7-stable_win64.exe --headless --path <fixture> --script res://tests/headless/resource_mutation_test.gd
```

Process 5444 exited normally with code 0. Complete output was the Godot `4.7.stable.official.5b4e0cb0f` banner and `resource mutation: PASS`; no script, parse, or runtime errors appeared.

Evidence retained at `C:/Users/PC/AppData/Local/Temp/hera-delta-qa-ddb7b13387834e81a230087a5d13927f/test.log`, alongside the exact copied sources.

| Case | Verified assertion |
|---|---|
| Dictionary supplied for Gradient offsets | Rejection; resource name and offsets remain original. |
| Integral JSON-compatible float `1.0` for integer interpolation mode | Accepted; interpolation mode becomes 1. |
| Fractional float `0.5` | Rejection; earlier resource name and interpolation mode remain unchanged. |
| Overflow float `1e30` | Rejection; earlier resource name remains unchanged. |

The same executed script also passed its existing resource/theme validation-before-mutation and successful theme persistence checks. The overflow assertion checks rejection and name preservation, not a separate interpolation-mode assertion.

## Separately consumed live evidence

Read the coordinator's `C:/Users/PC/AppData/Local/Temp/hera-fixes-live.fyDjpB/raw-probe-failure.json`: the resource batch step reports `ok:false`, `property expects PackedFloat32Array, got Dictionary`, and `stopped:true`. Read `raw-probe-after.json`: the Gradient retains `resource_name: original` and offsets `[0.0, 1.0]`. This is attributed live evidence, not an independently rerun live request. A batch process exit of 0 is its existing envelope behavior; the contained step's `ok:false` records rejection.

## Scope and cleanup

This recheck supplements [the prior full QA report](2026-09-08-final-qa.md), which records the full 16-regression suite and 26 scenario groups. Those broader checks were deliberately not repeated. No release, cross-version, or visual claim is made. The only Godot process started here exited normally; no owned process remains. Private artifacts are retained for review.
