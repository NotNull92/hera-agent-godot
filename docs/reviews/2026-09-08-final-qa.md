# Final independent QA — 2026-09-08

Verdict: **PASS within the tested scope**. No new defect found. This is QA evidence, not release approval.

Base SHA: `d6543c421e9b830301a8e0329c7ba56eaa2a1fd8` (HEAD independently confirmed).
Frozen dirty source SHA256 supplied by the coordinator: `81fa9cf375ea75f4fcf6c487b08768cf5862d47083243c14987ddda8ca873410`.
The base alone does not identify the tested changes. This lane made no source edits.

Reviewed [original findings](2026-09-07-code-review.md), [fix evidence](2026-09-08-review-fixes.md), `docs/DEV_MACHINE.md`, the scenario entry/validation flow, discovery isolation, and relevant regression assertions.

## Execution and evidence

- Independently built the current CLI with `go build -o <private-temp>/hera.exe .`; exit 0.
- Independently ran `go test ./cmd -count=1 -run 'TestGameQADiagnostics|TestGameQAScenario|TestGameQASummary|TestEvaluateGameQADiagnostics' -v`; exit 0. Seven top-level tests passed, including seven malformed-count subcases. This covers unavailable/missing/invalid diagnostics, omitted versus explicit-zero warning limits, preflight, explicit null, and successful-only coverage accounting.
- Executed the real binary as `hera.exe --json game qa --file <case.json>` under a private HOME/USERPROFILE. Eight malformed cases had a fresh synthetic heartbeat pointing to a private loopback TCP listener. Every case exited 2, wrote zero stdout bytes, reported the expected stderr validation error, and left the listener with no pending connection. Thus the leading `run` in later-invalid scenarios never reached transport. No real editor was used.
- After removing the synthetic heartbeat, a valid run scenario exited 1 with `no live Godot editor found` and empty stdout. `hera.exe --json instances` exited 0 and parsed as `{"count":0,"instances":[]}`. The JSON flag does not convert these usage/discovery stderr errors into JSON; this was observed, not assumed.
- Consumed the coordinator's completed fresh `tests/headless/run.sh` execution; did not duplicate it. Full log: `C:/Users/PC/AppData/Local/Temp/hera-final-regressions.log`. It ends `PASS: 16 standalone Godot regressions`. Runner requires every test exit 0 and rejects SCRIPT ERROR/Parse Error/Parser Error. Engine banner: Godot 4.7 stable `5b4e0cb0f`. Direct binary: `C:/Users/PC/Downloads/Godot_v4.7-stable_win64.exe/Godot_v4.7-stable_win64.exe`. Invocation uses `GODOT_BIN=<direct binary> KEEP=1 bash tests/headless/run.sh`; fixtures isolate project, HOME/USERPROFILE, APPDATA and XDG directories.
- The coordinator reports Go build/vet/shuffled tests and contracts passed; those broad gates are not claimed as independently executed by this lane. Earlier isolated graphical/live CLI checks are recorded in the fix evidence and were not rerun here.

## Scenario coverage

The 26 rows are scenario groups, not a claim of only 26 individual assertions. H rows are consumed completed execution evidence; C rows were executed independently in this lane. All passed.

| ID | Executed scenario group | Observation |
|---|---|---|
| H01 | diagnostics_read_test | Readable/unavailable diagnostics boundary regression passed. |
| H02 | game_autoload_lifecycle_test | Owned runtime autoload lifecycle regression passed. |
| H03 | game_clock_sequence_test | Frame stepping, ordered input, release/cancellation and concurrent mutation guards passed. |
| H04 | game_target_selection_test | Runtime target-selection regression passed. |
| H05 | game_ui_auditor_test | Runtime UI audit rule/filter regression passed. |
| H06 | http_server_test | Nonreading-client backpressure retained ticks, stalled partial output, expired connection; UTF-8, late replies and token loading checks passed. |
| H07 | main_scene_write_test | Engine main-scene serialization regression passed, including missing final newline. |
| H08 | node_remove_undo_test | Undo/redo ownership, nested ownership, saved descendants and root/outside rejection passed. |
| H09 | node_reparent_undo_test | Reparent undo regression passed. |
| H10 | resource_mutation_test | Rejected multi-property resource/theme edits preserved prior state; valid saved theme values passed. |
| H11 | runtime_geometry_test | Translated/scaled CanvasLayer, actual viewport input, camera bounds and cross-viewport rejection passed. |
| H12 | runtime_isolation_test | Actual viewport sizing and shared user-data marking passed. |
| H13 | runtime_request_test | Temporary/partial publication, once-only execution, response identity, wrong PID and synchronous self-free response passed. |
| H14 | script_language_test | Script language boundary regression passed. |
| H15 | script_template_csharp_test | C# generated fragments, safe types and invalid parameters passed. |
| H16 | script_validation_test | Fresh script-validation regression passed. |
| C01 | Malformed JSON `[{` | Exit 2; invalid scenario JSON; no connection. |
| C02 | Empty step array `[]` | Exit 2; at least one step required; no connection. |
| C03 | `run`, then unknown tool | Exit 2; step 2 unknown tool; no connection or earlier run. |
| C04 | `run`, then node get without path | Exit 2; step 2 requires path; no connection. |
| C05 | `run`, then node set without value | Exit 2; step 2 requires value; no connection. |
| C06 | `run`, then assertion with bad operator | Exit 2; step 2 unknown assertion op; no connection. |
| C07 | `run`, then diagnostics with warning limit -1 | Exit 2; nonnegative thresholds required; no connection. |
| C08 | Required behavior with no covers entry | Exit 2; requirement not covered; no connection. |
| C09 | Valid run scenario with empty private discovery | Exit 1; no live editor; empty stdout. |
| C10 | JSON instances with empty private discovery | Exit 0; valid JSON; count 0 and empty instances. |

## Limits and cleanup

The headless log contains editor shutdown RID/ObjectDB/resource warnings and errors in editor-mode fixtures; this is not a clean-console claim. No parser/script failures appear and the runner completed all 16 tests. No independent Godot 4.2, .NET compilation, export, race-instrumented, or pixel-level visual pass was performed here. The earlier graphical click/screenshot evidence remains separately attributed to the coordinator. Save-failure rollback remains outside the implementation guarantee.

The private listener was stopped in `finally`; its synthetic heartbeat was removed. This lane started no Godot process and never connected to the user's editor. CLI and fixture artifacts are deliberately retained for review, under the path below; no product or user-project changes were made.
Private CLI evidence: `C:\Users\PC\AppData\Local\Temp\hera-final-qa-583b9b4ef5684dca85405cbb2f0f3c8b` (cli-results.json, per-case JSON/stdout/stderr, go-targeted.log, fresh hera.exe).
