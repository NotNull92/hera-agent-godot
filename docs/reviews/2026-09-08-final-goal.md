# Final goal and contract review — 2026-09-08

Verdict: **FAIL** for the assigned frozen snapshot: R6 remains incomplete for native JSON values, as independently reproduced by the final quality lane. This is a source-and-evidence review, not an independent rerun of the coordinator's pending final gates or release approval.

Base SHA: `d6543c421e9b830301a8e0329c7ba56eaa2a1fd8`.
Assigned frozen source snapshot SHA256: `81fa9cf375ea75f4fcf6c487b08768cf5862d47083243c14987ddda8ca873410` (sorted changed/untracked non-document files, path + NUL + bytes + NUL, as recorded in the implementation evidence).

Reviewed the actual tracked diff and untracked implementation/regression files against `2026-09-07-code-review.md`, the approved `docs/superpowers/plans/2026-09-08-review-fixes.md`, and `2026-09-08-review-fixes.md`. Connected reads included CLI scenario execution and summary coverage, guarded project dispatch/default run, plugin polling/startup, runtime dispatch/PID handling, UI audit collection, shared path policy, and standalone test discovery.

| Requirement | Result and source evidence |
|---|---|
| R1 | PASS. `addons/hera_agent_godot/tools/node_tool.gd:199` snapshots subtree owners and restores them after reattachment. `tests/headless/node_remove_undo_test.gd:15` covers repeated undo/redo, saved descendants, and preservation of branch-owned children. |
| R2 | PASS. `node_tool.gd:205` rejects the resolved root and `node_tool.gd:344` excludes nodes outside the edited scene. The same regression covers direct/relative/absolute root aliases, outside nodes, and reparent containment. |
| R3 | PASS. `addons/hera_agent_godot/server/http_server.gd:53` retains writing state, advances bounded partial writes, and applies a separate absolute deadline. `tests/headless/http_server_test.gd:14` covers normal multi-chunk UTF-8 replies, auth/error replies, stalled readers, queued async work, expiration, and late/disconnected responses. The evidence explicitly avoids claiming that the original Windows socket reproduced a literal freeze. |
| R4 | PASS. `cmd/project.go:9` routes the setter through guarded editor dispatch; `addons/hera_agent_godot/tools/project_tool.gd:123` owns engine persistence. The corrupting text writer and compensating default-run disk reread are removed. `tests/headless/main_scene_write_test.gd:5` exercises the unterminated file; `cmd/project_rpc_test.go:17` and subsequent tests cover RPC output, rejection, default run, and multiple-editor refusal. The recorded isolated Old-to-New live round trip supplies the plan's required convergence evidence. |
| R5 | PASS. `cmd/game_qa.go:234` shares the diagnosis evaluator, which rejects unavailable/malformed counts; `cmd/game_qa_scenario.go:37` represents warning omission separately. `cmd/game_qa_review_test.go:13` checks that unreliable diagnostics cannot satisfy successful scenario coverage and distinguishes omitted warnings from explicit zero. |
| R6 | FAIL. Staging exists, but `addons/hera_agent_godot/tools/resource_value_codec.gd:35` accepts incompatible non-string native JSON values without type validation. The quality lane reproduced a valid resource name plus Dictionary-valued Gradient offsets returning success while clearing offsets. Existing regressions cover invalid names/string values but miss this path. See blocker Q1 in `2026-09-08-final-quality.md`. Theme staging and resource string staging are present; disk-save rollback remains separate. |
| R7 | PASS. `addons/hera_agent_godot/tools/game_tool.gd:63` writes/closes a unique temporary file before publication. `addons/hera_agent_godot/runtime/game_inspector.gd:63` consumes only a parsed dictionary, before dispatch. `tests/headless/runtime_request_test.gd:17` checks ignored temporary files, partial-file survival, exactly-once completed dispatch, response IDs and wrong-PID rejection. |
| R8 | PASS. `addons/hera_agent_godot/runtime/game_ui_geometry.gd:3` shares transformed local geometry across click targeting, tree summaries, and audit checks. Semantic clicks reject another viewport at `game_ui_inspector.gd:89`. `tests/headless/runtime_geometry_test.gd:14` covers translated/scaled CanvasLayer bounds and actual input, camera geometry, audit classifications, and cross-viewport rejection. The evidence also records an isolated graphical CLI click and matching-size capture. |
| R9 | PASS. `game_inspector.gd:219` captures the resolved path before arbitrary user code. The runtime request regression calls native synchronous `free` and checks the surviving response path. |
| R10 | PASS. `http_server.gd:206` distinguishes missing/empty tokens from open/read failures. `addons/hera_agent_godot/hera_agent_plugin.gd:90` returns before server/heartbeat startup on failure. Token tests cover missing/empty, file/env precedence and unreadable paths; implementation evidence records the real Windows exclusive-lock startup check. `docs/SECURITY.md:72` protects both existing and newly created POSIX token files. |
| R11 | PASS. `.github/workflows/ci.yml:120` invokes the discovered standalone tests on Godot 4.7. `tests/headless/run.sh:24` creates a separate project/home per test, chooses editor mode from explicit markers, bounds execution, and rejects script/parser failures. Existing static version-floor jobs remain separate. |

Apart from the R6 blocker, the refactoring intent is implemented without a new dependency or architecture: existing project-path guards replace duplicated lexical helpers; pure Variant hints and existing input-name conversions are reused; diagnostics and viewport geometry have shared interpretation. `cmd/game_qa_preflight.go:5` validates the agreed bounded fields before discovery/requests. It is not a complete schema for every nested tool parameter, and `docs/CONTRACT.md:279` states that boundary. No requirement to split large files mechanically was introduced.

README EN/KO, commands, contract, roadmap, security, and architecture documentation are synchronized with the changed behavior. The lower-priority diagnostic/output read-error boundary and discovery retry documentation were also addressed. No MCP surface, release/version bump, third-party publication, or dependency change appears in the reviewed diff.

Final coordinator gates and final evidence-document status were still pending when this lane reviewed them. This FAIL applies to the stamped original snapshot; a focused fix requires a fresh delta review and updated evidence before completion. The documented limitations remain applicable: no disk-save rollback guarantee, no fresh local Godot 4.2 execution or race run, and retained headless shutdown warnings. No implementation files were edited by this lane.
