# v0.9.0 Godot Asset Store Upload Prep

## Release State

- Target version: `v0.9.0`
- Addon manifest version: `0.9.0`.
- GitHub Release: `https://github.com/NotNull92/hera-agent-godot/releases/tag/v0.9.0` (published 2026-07-14; release workflow run 29310410241 succeeded, `go test -race` included)
- Release asset URL: `https://github.com/NotNull92/hera-agent-godot/releases/download/v0.9.0/hera-agent-godot-addon.zip`
- Source snapshot: the `v0.9.0` tag.
- Upload target: Godot Asset Store new-version entry.
- Store upload: uploaded 2026-07-14 (Min `Godot 4.2` / Max `Godot 4.7`,
  Stable; Settings Summary/Description replaced). Approval pending.
- Theme: ROADMAP Phase 8 — reach & agent-side distribution (npm/Homebrew/Scoop
  install, Claude Code + Codex plugins and a Cursor rule sharing one
  ~1k-token `live-editor` skill, a copyable nonvisual CI tier, and a live demo
  GIF), with two addon-facing refinements: value-syntax hints in set errors and
  a steadier `game qa` runtime lifecycle. The verified Godot range is unchanged
  from v0.8.0 (4.2–4.7).

## Upload Artifact

Verified against the GitHub Release `checksums.txt` on 2026-07-14.

- Local upload ZIP: `dist/release-v0.9.0/hera-agent-godot-addon.zip`
  (downloaded from the GitHub Release assets)
- Local ZIP SHA256: `a025f9188d0a81cba400f9c0ed96c661de2a2c08604fdb25ea5ebabffdf501aa`
- Release ZIP SHA256 (`checksums.txt`): `a025f9188d0a81cba400f9c0ed96c661de2a2c08604fdb25ea5ebabffdf501aa` (match verified)
- ZIP root: `addons/hera_agent_godot/...`
- ZIP entry count: `100`
- Required entries verified:
  - `addons/hera_agent_godot/plugin.cfg` (embedded manifest `version="0.9.0"`)
  - `addons/hera_agent_godot/LICENSE`
  - `addons/hera_agent_godot/hera_agent_plugin.gd`
- Duplicate root `LICENSE`: not present.
- Non-addon repository folders (`docs/`, `cmd/`, `internal/`, `scenes/`, `.github/`): not present (0 entries outside `addons/hera_agent_godot/`).

## Store Fields

The Summary/Description are asset-wide on the **Settings** tab; the file upload
happens on the **Versions** tab, and each saves separately. Existing published
version entries: `v0.8.0` (2026-07-13, Min `Godot 4.2` / Max `Godot 4.7`,
Stable), `v0.7.0`, and `v0.6.0`. Replace the Settings Summary/Description with
the v0.9.0 blocks below.

**Settings tab:**

- Asset name: `Hera Agent Godot` (keep)
- Asset Summary: replace with the block below
- Detailed Description: replace with the block below
- Tags: keep the current `AI`, `LLM`, `AI Agent`, `Automation`, `CLI`
  (the store shows at most 5 tags)
- Type: `Addon` (keep)
- License: `MIT` (keep)
- Source Code URL: `https://github.com/NotNull92/hera-agent-godot` (keep)
- AI usage disclosure: keep checked; explanation: AI assistance was used
  during development and documentation; no generated art, audio, or runtime
  media assets are bundled in the addon package.
- Press **Save**.

**Versions tab — Upload A New Version:**

- File: `dist/release-v0.9.0/hera-agent-godot-addon.zip` (the store hosts
  the file itself and requires a license file inside the zip — ours ships
  `addons/hera_agent_godot/LICENSE`)
- Version Name: `v0.9.0`
- Version Changelog: paste the Changelog section below (Markdown supported)
- Stable: checked (matches the existing entries)
- Minimum Required Godot Version: `Godot 4.2` — unchanged from v0.8.0; every
  stable from 4.2 to 4.7 is verified (see
  [SUPPORT_MATRIX.md](../SUPPORT_MATRIX.md)); 4.7 remains the fully-QA'd
  recommendation.
- Maximum Compatible Godot Version: `Godot 4.7` (match the v0.8.0 entry; raise
  or clear when a newer stable is verified)
- Additional Information (≤200 chars, optional): `v0.9.0 focuses on reach:
  install via npm/Homebrew/Scoop, Claude Code + Codex agent plugins, clearer
  value-syntax errors, and a steadier game-QA lifecycle.`
- Press **Upload**.

## Asset Summary

Paste verbatim into the Asset Store **Asset Summary** field (491 / 500 chars).

```text
The in-editor bridge for Hera — a low-token CLI that lets AI coding agents inspect and control a live Godot editor: read output/errors, run scenes, edit the node tree, wire signals, inject input, and QA a running game with proof. Verified on Godot 4.2–4.7. New in v0.9.0: value-syntax errors name the Godot variant text to use, game-QA is steadier, and the CLI installs via npm, Homebrew, or Scoop and drops into Claude Code or Codex as a plugin. MCP-grade reach at a fraction of the tokens.
```

## Detailed Description

Paste verbatim into the Asset Store **Detailed Description** field (Markdown, 5994 / 20000 chars).

```markdown
**Hera Agent Godot** lets AI coding agents inspect and control a **live Godot editor** in real time — instead of guessing scene structure from stale training data, the agent acts on the *real* editor and checks the result. **Verified on every stable from Godot 4.2 to 4.7.**

This Asset Library entry is the **editor addon** (the in-editor bridge). The companion **`hera` CLI** lives on GitHub and is what the agent actually runs.

## Why a CLI, not MCP?

Godot already has a healthy MCP-addon ecosystem — Hera makes the opposite bet on purpose. MCP servers pay for breadth in tokens: dozens to 100+ tool schemas plus verbose JSON responses sit in the agent's context every turn.

Hera delivers **MCP-grade reach over the live editor as a compact-JSON-by-default CLI** — one command per action, minimal tokens, and it works with anything that can run a shell command (pipes, `batch`, CI, any agent), not just MCP clients. The agent loads **zero tool schemas**: the only resident surface is one small cacheable doc (~1k tokens) that stays flat no matter how many commands exist, versus an estimated ~4k–31k resident tokens for sampled Godot MCP servers.

## What the agent can do

- **Read the editor** — live status, scene tree, node property dumps, open scenes, editor selection, and ClassDB inspection.
- **Edit the scene** — add / set / remove nodes, attach & detach scripts, wire resources, connect & disconnect signals. Every write is a single undoable step (Ctrl+Z).
- **Scripts & project** — create scripts, make folders, list and scan project files, inspect and refresh resources and UIDs.
- **Run GDScript** — evaluate an expression against the edited scene through an `EditorInterface` proxy.
- **Drive a running game** — inspect and mutate the runtime tree, call helper methods, click controls semantically, inject real input and prove what was delivered, read the runtime UI tree, and assert on state.
- **Proof-first QA** — capture and analyze screenshots, run requirement-covered QA scenarios from a file, and get a one-shot runtime health verdict with `game qa diagnose`.
- **Diagnostics** — tail the Godot log with error/warning filters and summarize problems.

## New in v0.9.0 — reach, and clearer feedback

- **Value-syntax hints in errors.** When a `node set`, `game node set`, or `resource set` value can't be parsed, the error now names the exact Godot variant text to use — e.g. `Vector2(x, y)`, a *flat* `PackedVector2Array(x1, y1, x2, y2, …)`, `Color(r, g, b, a)`, or `node set-resource` for object/resource properties — instead of failing without guidance. Agents recover in one turn instead of guessing.
- **Steadier game-QA lifecycle.** `game qa` run/stop steps now confirm the runtime actually started or stopped (and the game instance cleared) before moving on, bound `wait` durations, and compare numeric `eq`/`ne` assertions numerically rather than as strings — so the run → inspect → QA → stop flow is reliable, including under CI.
- **Agent-side install.** Get the `hera` CLI from the [`hera-godot` npm package](https://www.npmjs.com/package/hera-godot), a Homebrew tap, or an in-repo Scoop bucket, in addition to the one-line installers.
- **Agent harness kits.** Drop Hera into a coding agent with the Claude Code plugin and the Codex plugin — both sharing one auto-invoked, ~1k-token `live-editor` skill — plus a Cursor rule template and a copy-paste `AGENTS.md` snippet.
- **Copyable nonvisual CI tier.** A pinned Godot 4.7 headless lifecycle (enabled editor → `smoke` → `game qa` runtime logic) plus a static 4.2/4.7 script gate, documented so you can run the same verification on your own fork.

## Command surface

`status`, `instances`, `run` / `stop`, `scene`, `editor`, `script`, `project`, `classdb`, `node` (read + write + resource/script wiring), `signal`, `resource`, `game` (runtime inspect + input + input-log + set/call/click + assert + QA + screenshot), `guidance`, `game_feel`, `output`, `diagnostics`, `eval`, `screenshot`, `batch`, and `smoke` — with `--json` (pretty) and `--ids` (paths only) output modes plus `--instance` and `--timeout` global flags; compact JSON is the default.

## How it works

The addon is an `@tool` `EditorPlugin` that runs a localhost HTTP server and advertises the editor to the CLI via `~/.hera-agent-godot/instances/`. The Go CLI discovers the editor and sends one compact JSON request per command, executed on the editor main thread through `EditorInterface`. No cloud and no account — everything stays on localhost, optionally behind the shared token.

## Install

1. Use any **Godot 4.2–4.7** stable build (4.7 recommended — it gets the full QA treatment; see `docs/SUPPORT_MATRIX.md` in the repo).
2. Copy `hera_agent_godot/` into your project's `res://addons/`.
3. Enable **Project → Project Settings → Plugins → Hera Agent Godot**.
4. Grab the `hera` CLI from GitHub (one-line installer, `npm`, `brew`, `scoop`, or `go build`) and start driving the editor from your agent.

## License

MIT-licensed — source, output contract, support matrix, agent plugins, and the measured low-token methodology all live on GitHub.

## Repo

If Hera saves your agent tokens, a star helps other Godot devs find it 🌟
**[github.com/NotNull92/hera-agent-godot](https://github.com/NotNull92/hera-agent-godot)**

## Links

- 🛒 Godot Asset Store: [store.godotengine.org/asset/notnull92/hera-agent-godot](https://store.godotengine.org/asset/notnull92/hera-agent-godot/)
- 📦 npm: [npmjs.com/package/hera-godot](https://www.npmjs.com/package/hera-godot)
- 🍺 Homebrew tap: [github.com/NotNull92/homebrew-hera](https://github.com/NotNull92/homebrew-hera)
- 📺 YouTube — Indie Alchemist: [youtube.com/@IndieAlchemist](https://www.youtube.com/@IndieAlchemist)
- 💬 Discord community: [discord.gg/QBzEVuYwK](https://discord.gg/QBzEVuYwK)
- 🎮 Unity sibling — hera-agent-unity: [github.com/NotNull92/hera-agent-unity](https://github.com/NotNull92/hera-agent-unity)
- ☕ Support on Ko-fi: [ko-fi.com/notnull92](https://ko-fi.com/notnull92)
```

## Changelog

- Added value-syntax hints to `node set`, `game node set`, and `resource set`
  errors: when a value can't be coerced, the message names the expected Godot
  variant text (e.g. `Vector2(x, y)`, a flat `PackedVector2Array(...)`,
  `Color(r, g, b, a)`) and points object/resource properties at
  `node set-resource`, across the node, game, and resource value codecs.
- Stabilized the `game qa` runtime lifecycle: `run`/`stop` steps now require a
  successful response and confirm the game actually started or fully stopped
  (game instance cleared) before continuing, `wait` durations are bounded, and
  numeric `eq`/`ne` assertions compare numerically instead of as strings.
- Added agent-side CLI distribution: the `hera-godot` npm wrapper package
  (postinstall downloads the pinned, SHA256-verified release binary), a
  Homebrew tap, and an in-repo Scoop bucket, alongside the existing one-line
  installers.
- Added agent harness kits: Claude Code and Codex repo marketplaces/plugins
  sharing one auto-invoked `live-editor` skill, a Cursor rule template, and a
  copy-paste `AGENTS.md` snippet — each within the ~1k-token single-document
  surface.
- Added a documented nonvisual CI tier (`docs/HEADLESS_CI.md`): a pinned Godot
  4.7 headless editor → `smoke` → `game qa` runtime-logic lifecycle plus a
  static 4.2/4.7 addon-script gate.
- Added a live demo: a README walkthrough asset and recording script
  (`scenes/demo/`) and an embedded playtest GIF that builds an arena + player
  from an empty scene, runs it, reads the moving player, runs QA, and recolors
  a running node for ~1,170 tokens of tool output.
- Added canonical upstream Godot source references to the contributor docs and
  expanded the roadmap's standardization arc (Phases 7–9).

## Validation

Run against the current tree (pre-bump), with the live Godot 4.7 dev editor:

- `go test -shuffle=on -count=1 ./...` passed.
- `go vet ./...` passed.
- `git diff --check` passed.
- Godot 4.7 headless `--check-only --script` passed for the changed addon
  scripts (node/game/resource value codecs, `game_assertions.gd`).
- `go run . diagnostics` passed with `clean:true`, `error_count:0`,
  `warning_count:0`.
- `go run . smoke --skip-game` passed for status, diagnostics, and scene.
- `go run . smoke --run-game` passed (run → runtime inspect → stop).
- `go test -race` runs in CI (the release workflow gates on it before
  publishing assets).

## Remaining Steps

1. ~~Bump the addon manifest to `0.9.0` and refresh the README
   release-baseline sections (EN/KO/addon).~~ Done.
2. ~~Commit `Prepare v0.9.0 release`, tag `v0.9.0`, push the tag — the release
   workflow builds the CLI binaries, the addon zip, and `checksums.txt`.~~
   Done 2026-07-14 (commit `3a70b09`, tag `v0.9.0`, run 29310410241 success).
3. ~~Fill the Upload Artifact section above from `checksums.txt`.~~ Done — SHA256
   `a025f918…f501aa`, 100 entries, embedded manifest `0.9.0`, 0 entries outside
   the addon, no duplicate root LICENSE.
4. ~~**Owner:** upload `hera-agent-godot-addon.zip` (from the GitHub Release
   assets) on the Godot Asset Store Versions tab, Version Name `v0.9.0`,
   Min `Godot 4.2` / Max `Godot 4.7`, Stable.~~ Done 2026-07-14 (approval
   pending).
5. ~~**Owner:** replace the **Settings**-tab Asset Summary and Detailed
   Description with the v0.9.0 blocks above and press Save.~~ Done 2026-07-14.
6. ~~Check the ROADMAP Phase 8 box for "Asset Store upload `v0.9.0`".~~ Done.
7. ~~Bump and republish the `hera-godot` npm wrapper to `0.9.0` (update the six
   `packaging/npm/manifest.json` SHA256 values from this release's
   `checksums.txt` and `package.json` version, then `npm publish` — owner).~~
   Done 2026-07-14 — published and verified end-to-end (`npm view hera-godot
   version` → `0.9.0`; fresh install runs postinstall, SHA-verifies the binary,
   and reports `v0.9.0`). Homebrew tap `Formula/hera.rb` also updated to 0.9.0
   (commit `df33553` in `NotNull92/homebrew-hera`).
