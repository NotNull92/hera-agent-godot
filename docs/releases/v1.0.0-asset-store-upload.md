# v1.0.0 Godot Asset Store Upload Prep

## Release State

- Target version: `v1.0.0`
- Addon manifest version: `1.0.0`.
- GitHub Release: `https://github.com/NotNull92/hera-agent-godot/releases/tag/v1.0.0`
  (published 2026-07-21; release workflow run 29818488453 passed, including
  `go test -race`)
- Release asset URL: `https://github.com/NotNull92/hera-agent-godot/releases/download/v1.0.0/hera-agent-godot-addon.zip`
- Source snapshot: the `v1.0.0` tag (commit `10f245d`).
- Upload target: Godot Asset Store new-version entry.
- Store upload: submitted manually by the owner on 2026-07-21 (Min
  `Godot 4.2` / Max undefined, Stable). The public Store page still listed
  `v0.9.0` after submission, so v1.0.0 processing or public propagation
  remained to be observed.
- Theme: declare Hera's stable v1 CLI contract and SemVer policy; add Godot
  `Theme` resource inspection and mutation, local screenshot comparison, and
  Godot-native UI theme QA; harden editor discovery and make diagnostics
  truthful when log evidence is unavailable. The verified Godot range remains
  4.2–4.7, with 4.7 as the fully QA'd baseline.

## Upload Artifact

Verified against the GitHub Release `checksums.txt` on 2026-07-21.

- Local upload ZIP: `dist/release-v1.0.0-gh/hera-agent-godot-addon.zip`
  (downloaded from the GitHub Release assets)
- Local ZIP SHA256: `0a71000f0c4192043e72e9b18f4de3bac720035d9d7c95c9634648a7b5c54d9f`
- Release ZIP SHA256 (`checksums.txt`): `0a71000f0c4192043e72e9b18f4de3bac720035d9d7c95c9634648a7b5c54d9f`
  (match verified)
- ZIP root: `addons/hera_agent_godot/...`
- ZIP entry count: `102` (`94` files plus directory entries)
- Required entries verified:
  - `addons/hera_agent_godot/plugin.cfg` (embedded manifest
    `version="1.0.0"`)
  - `addons/hera_agent_godot/LICENSE`
  - `addons/hera_agent_godot/hera_agent_plugin.gd`
- Duplicate root `LICENSE`: not present.
- Non-addon repository folders (`docs/`, `cmd/`, `internal/`, `scenes/`,
  `.github/`): not present (0 entries outside
  `addons/hera_agent_godot/`).

## Store Fields

The Summary/Description are asset-wide on the **Settings** tab; the file upload
happens on the **Versions** tab, and each saves separately. The owner submitted
the v1.0.0 version on 2026-07-21. Replace the Settings Summary/Description with
the v1.0.0 blocks below if the Store still shows the older v0.9.0 metadata.

**Settings tab:**

- Asset name: `Hera Agent Godot` (keep)
- Asset Summary: replace with the block below
- Detailed Description: replace with the block below
- Tags: keep the current `AI`, `LLM`, `AI Agent`, `Automation`, `CLI`
  (the Store shows at most 5 tags)
- Type: `Addon` (keep)
- License: `MIT` (keep)
- Source Code URL: `https://github.com/NotNull92/hera-agent-godot` (keep)
- AI usage disclosure: keep checked; explanation: AI assistance was used
  during development, documentation, and QA; no generated art, audio, or
  runtime media assets are bundled in the addon package.
- Press **Save**.

**Versions tab — Upload A New Version:**

- File: `dist/release-v1.0.0-gh/hera-agent-godot-addon.zip` (the Store hosts
  the file itself and requires a license file inside the ZIP — ours ships
  `addons/hera_agent_godot/LICENSE`)
- Version Name: `v1.0.0`
- Version Changelog: paste the Changelog section below (Markdown supported)
- Stable: checked
- Minimum Required Godot Version: `Godot 4.2` — unchanged from v0.9.0; every
  stable from 4.2 to 4.7 is verified (see
  [SUPPORT_MATRIX.md](../SUPPORT_MATRIX.md)); 4.7 remains the fully QA'd
  recommendation.
- Maximum Compatible Godot Version: leave blank, matching the submitted
  v1.0.0 entry; set a maximum only if Store policy requires one.
- Additional Information (170 / 200 chars, optional): `v1 freezes Hera's stable
  CLI contract, adds Theme resource QA and local screenshot diff, and hardens
  editor discovery and diagnostics. Upgrade the CLI and addon together.`
- Press **Upload**.

## Asset Summary

Paste verbatim into the Asset Store **Asset Summary** field (472 / 500 chars).

```text
The in-editor bridge for Hera, a low-token CLI that lets AI coding agents inspect and control a live Godot editor: edit scenes, wire signals, inject input, compare screenshots, and QA running games with proof. Verified on Godot 4.2–4.7. v1 freezes the stable CLI contract with SemVer, adds Godot Theme resource QA and local screenshot diff, and hardens editor discovery and diagnostics. CLI installers and agent plugins are available from GitHub, npm, Homebrew, and Scoop.
```

## Detailed Description

Paste verbatim into the Asset Store **Detailed Description** field (Markdown,
6931 / 20000 chars).

```markdown
**Hera Agent Godot** lets AI coding agents inspect and control a **live Godot editor** in real time — instead of guessing scene structure from stale training data, the agent acts on the *real* editor and checks the result. **Verified on every stable from Godot 4.2 to 4.7.**

This Asset Library entry is the **editor addon** (the in-editor bridge). The companion **`hera` CLI** lives on GitHub and is what the agent actually runs.

## Why a CLI, not MCP?

Godot already has a healthy MCP-addon ecosystem — Hera makes the opposite bet on purpose. MCP servers pay for breadth in tokens: dozens to 100+ tool schemas plus verbose JSON responses sit in the agent's context every turn.

Hera delivers **MCP-grade reach over the live editor as a compact-JSON-by-default CLI** — one command per action, minimal tokens, and it works with anything that can run a shell command (pipes, `batch`, CI, any agent), not just MCP clients. The agent loads **zero tool schemas**: the only resident surface is one small cacheable doc (~1k tokens) that stays flat no matter how many commands exist, versus an estimated ~4k–31k resident tokens for sampled Godot MCP servers.

## What the agent can do

- **Read the editor** — live status, scene tree, node properties, open scenes, selection, focused scripts, resources, Theme items, and ClassDB metadata.
- **Edit the scene** — add / instance / set / remove nodes, attach & detach scripts, wire resources, connect & disconnect signals. Editor node and signal writes are undoable steps (Ctrl+Z).
- **Scripts & project** — create scripts and scenes, manage project files and folders, scan/reimport resources, refresh UIDs, and set the main scene.
- **Inspect and edit Themes** — read or update Godot `Theme` colors, constants, and font sizes through the engine's per-type Theme maps.
- **Run GDScript** — evaluate an expression against the edited scene through an `EditorInterface` proxy.
- **Drive a running game** — inspect and mutate the runtime tree, call helper methods, click controls semantically, inject real input and prove what was delivered, read the runtime UI tree, and assert on state.
- **Proof-first QA** — capture and analyze screenshots, compare captures locally, run requirement-covered QA scenarios, and get a one-shot runtime health verdict with `game qa diagnose`.
- **Godot-native UI theme QA** — measure spacing, type scale, color convergence, contrast, container structure, and decoration, then re-read live predicates and confirm the rendered result.
- **Diagnostics** — tail the effective Godot log with error/warning filters, expose unavailable logging evidence, and summarize problems without false-clean results.

## New in v1.0.0 — a stable contract, Theme QA, and stronger proof

- **Stable v1 CLI contract.** Hera now freezes its documented stable command invocation, output streams, exit-code meanings, response field names, and JSON types for the v1 major line, backed by contract goldens, SemVer, and a written deprecation policy.
- **Godot Theme resource tools.** `theme get` and `theme set` read and update the colors, constants, and font sizes stored behind Godot's per-type `Theme` maps — data that ordinary resource property mutation cannot reach.
- **Local screenshot comparison.** `screenshot diff` compares captures without a live editor and reports dimensions, changed-pixel count and ratio, maximum channel delta, identity, and the exact changed bounding box, with a threshold for anti-aliasing wobble.
- **Godot-native UI theme QA.** The bundled workflow measures spacing, type scale, color convergence, WCAG contrast, container structure, and decoration. Safe mechanical fixes use existing project values and are verified against the live rendered result; structural and stylistic judgment remains report-only.
- **More reliable editor discovery.** Empty instance scans retry over bounded delays during Windows heartbeat replacement races, while repeated heartbeat failures still surface before the freshness window is at risk.
- **Truthful, quieter diagnostics.** `diagnostics`, `output`, and `game qa diagnose` report when log evidence is unavailable instead of returning a false clean result, while expected mid-write runtime file-exchange races no longer flood the Godot output.
- **Compatible v0.9 → v1 upgrade.** Upgrade the CLI and addon together, fully restart Godot, then run `hera status` and `hera smoke --skip-game`. Existing scenes, opt-in auth, discovery paths, and the transitional `hera-agent-godot` command alias remain compatible.

## Command surface

`status`, `instances`, `run` / `stop`, `scene`, `editor`, `script`, `project`, `classdb`, `node` (read + write + resource/script wiring), `signal`, `resource`, `theme`, `game` (runtime inspect + input + input-log + set/call/click + assert + QA + screenshot), `guidance`, `game_feel`, `output`, `diagnostics`, `eval`, `screenshot` (capture + local diff), `batch`, and `smoke` — with `--json` (pretty) and `--ids` (paths only) output modes plus `--instance` and `--timeout` global flags; compact JSON is the default.

## How it works

The addon is an `@tool` `EditorPlugin` that runs a localhost HTTP server and advertises the editor to the CLI via `~/.hera-agent-godot/instances/`. The Go CLI discovers the editor and sends one compact JSON request per command, executed on the editor main thread through `EditorInterface`. No cloud and no account — everything stays on localhost, optionally behind the shared token.

## Install

1. Use any **Godot 4.2–4.7** stable build (4.7 recommended — it gets the full QA treatment; see `docs/SUPPORT_MATRIX.md` in the repo).
2. Copy `hera_agent_godot/` into your project's `res://addons/`.
3. Enable **Project → Project Settings → Plugins → Hera Agent Godot**.
4. Grab the `hera` CLI from GitHub (one-line installer, `npm`, `brew`, `scoop`, or `go build`) and start driving the editor from your agent.

## License

MIT-licensed — source, output contract, support matrix, migration guide, agent plugins, and the measured low-token methodology all live on GitHub.

## Repo

If Hera saves your agent tokens, a star helps other Godot devs find it 🌟
**[github.com/NotNull92/hera-agent-godot](https://github.com/NotNull92/hera-agent-godot)**

## Links

- 🛒 Godot Asset Store: [store.godotengine.org/asset/notnull92/hera-agent-godot](https://store.godotengine.org/asset/notnull92/hera-agent-godot/)
- 📦 npm: [npmjs.com/package/hera-godot](https://www.npmjs.com/package/hera-godot)
- 🍺 Homebrew tap: [github.com/NotNull92/homebrew-hera](https://github.com/NotNull92/homebrew-hera)
- 📺 YouTube — Indie Alchemist: [youtube.com/@IndieAlchemist](https://www.youtube.com/@IndieAlchemist)
- 💬 Discord community: [discord.gg/QBzEVuYwK](https://discord.gg/QBzEVuYwK)
- 🎮 Unity sibling — hera-agent-unity: [github.com/NotNull92/hera-agent-unity](https://github.com/NotNull92/hera-agent-unity)
- ☕ Support on Ko-fi: [ko-fi.com/notnull92](https://ko-fi.com/notnull92)
```

## Changelog

### Stable v1 contract and upgrade path

- Froze the documented **stable** CLI surface for the v1 major line: command
  invocation, output streams, exit-code meanings, response field names, and
  JSON types now carry a compatibility promise backed by contract goldens.
- Adopted semantic versioning and a written deprecation policy. Stable fields
  may grow additively, but removal or incompatible change requires deprecation
  in a minor release and waits until the next major release.
- Added a v0.9-to-v1 migration guide. The upgrade is intentionally compatible:
  update the CLI and addon together, fully restart Godot, and keep existing
  scenes, scripts, shared-token settings, discovery paths, and the transitional
  `hera-agent-godot` command alias.
- Kept the new `theme` and `screenshot diff` responses explicitly
  **experimental** so their shapes can mature without weakening the stable v1
  contract.

### Theme resources and visual QA

- Added `theme get <res://theme.tres> [--type <ThemeType>]` to read colors,
  constants, and font sizes from Godot `Theme` per-type maps that ordinary
  resource property inspection cannot reach.
- Added `theme set` with repeatable `--color`, `--constant`, and `--font-size`
  items. It saves the Theme resource, reports `undoable:false`, rejects unknown
  theme types and empty writes, and prints colors to six decimals so float32
  values round-trip without noisy tails.
- Added local `screenshot diff <before> <after> [--threshold N]`. It works
  without an editor and reports dimensions, changed-pixel count and ratio,
  maximum channel delta, identity, and the exact changed bounding box.
- Made screenshot comparison tolerant of anti-aliasing wobble with a per-channel
  threshold while still reporting `max_delta` below that threshold; mismatched
  dimensions and unreadable or invalid images fail instead of producing a
  misleading comparison.
- Shipped the Godot-native `ui-theme-qa` skill in both Claude Code and Codex
  plugin trees. It measures six areas: spacing, type scale, color convergence,
  WCAG contrast, container structure, and decoration.
- The four mechanical areas update undoable per-node `theme_override_*` values
  in dependency order, then re-read live predicates and confirm the rendered
  result. Container and decoration findings remain report-only because deleting
  or re-parenting nodes can break semantic dividers, brand marks, tooltips, and
  script node paths.
- Rooted the reference corpus in Godot's own default theme: 4 px spacing,
  16/20/24/28 font anchors, and the engine's 1.25 heading step. WCAG remains the
  accessibility authority; no outside color ramp is imported over a project's
  visual language.
- Added safe color convergence that chooses the project's most-used existing
  value for near-duplicates, and contrast repair that preserves hue, computes
  both reachable lightness bounds, and reports when neither bound is valid.
- Fixed real mixed-UI inspection failures by reading only the theme tokens each
  Control class actually defines, and fixed type-scale collision handling so a
  hierarchy correction does not shrink unrelated peer controls.
- Deliberately excluded automatic wholesale restyling. Hera can apply a chosen
  style through `node set`, `theme set`, and `resource create Theme`, but the QA
  workflow does not invent taste or silently replace a project's design.

### Live-editor reliability and truthful diagnostics

- Hardened Windows editor discovery against the non-atomic heartbeat rename
  window. Empty scans now retry over growing 25/50/100/200 ms delays and stop
  at the first live instance; the final QA sweep passed 39/39 discovery calls
  and 10/10 repeated `editor state` calls.
- Reduced heartbeat warning noise without hiding real failures: a single
  publish collision is treated as expected, while eight consecutive failures
  still emit one warning before the five-second freshness window is at risk.
- Replaced noisy `JSON.parse_string()` calls with quiet, checked parsing at the
  runtime request, response, game-instance, and Game Feel line-reader polling
  boundaries. Mid-write files now retry without printing false engine errors;
  malformed HTTP request bodies remain real protocol errors.
- Made `diagnostics` and `output` read Godot's effective feature-tagged logging
  setting and path. An unreadable log now returns `available:false` with a hint,
  and diagnostics returns `clean:false` instead of claiming a project is clean
  from missing evidence.
- Updated `game qa diagnose` to fail the editor-diagnostics check when the log
  is explicitly unavailable, while preserving compatibility with older addons
  that do not return the new `available` field.
- Corrected the documented visibility boundary: normal file logging covers the
  running project, not the editor console. Launching the editor with
  `--log-file <path>` is the supported way to capture editor output.
- Documented that disabling and re-enabling the plugin does not reload preloaded
  addon scripts; a full editor restart is required before a live smoke can prove
  addon source changes.

### Release integrity and documentation

- Added the missing tracked UID sidecar for `theme_tool.gd`, preventing Godot
  from regenerating different script UIDs across clones.
- Synchronized English/Korean command documentation, the architecture and
  output contracts, the UI Theme QA design record, support evidence, release
  baseline, and Asset Store form copy for the v1 declaration.

## Validation

- `gofmt` check, `go test ./...`, `go build ./...`, `go vet ./...`, and
  `go test -shuffle=on -count=1 ./...` passed.
- Godot 4.7 headless `--check-only` passed for all 44 addon GDScript files.
- Live Hera `diagnostics`, `smoke --skip-game`, and `smoke --run-game` passed;
  runtime cleanup left zero game instances.
- `git diff --check` passed; the new migration document passed markdownlint.
- The repository's existing long-form Markdown files still report pre-existing
  markdownlint style findings for tables, inline HTML, and line length; no
  release-prep diff whitespace errors are present.
- Local `go test -race` was not run because antivirus blocks race-instrumented
  binaries on this machine; the GitHub Release workflow gates publishing on
  `go test -race -shuffle=on -count=1 ./...`.
- Release workflow
  [run 29818488453](https://github.com/NotNull92/hera-agent-godot/actions/runs/29818488453)
  passed on 2026-07-21, including race tests, six CLI builds, addon packaging,
  checksums, and GitHub Release publication.

## Remaining Steps

1. ~~Bump the addon manifest to `1.0.0`, synchronize the stable-contract,
   migration, support, architecture, command, and release-baseline docs, and
   prepare the Asset Store copy.~~ Done.
2. ~~Commit the release, tag `v1.0.0`, and publish the GitHub Release assets
   through the release workflow.~~ Done 2026-07-21 (commit `10f245d`, release
   workflow run 29818488453 passed).
3. ~~Verify the release addon ZIP against `checksums.txt`.~~ Done — SHA256
   `0a71000f…54d9f`, 102 entries, embedded manifest `1.0.0`, 0 entries outside
   the addon, and no duplicate root `LICENSE`.
4. ~~**Owner:** upload `hera-agent-godot-addon.zip` on the Godot Asset Store
   Versions tab, Version Name `v1.0.0`, Min `Godot 4.2` / Max undefined,
   Stable.~~ Done 2026-07-21; Store processing or public propagation remained
   pending at the last recorded check.
5. ~~**Owner:** replace the **Settings**-tab Asset Summary and Detailed
   Description with the v1.0.0 blocks above and press Save.~~ Submitted
   2026-07-21; recheck the public page because it still showed the older v0.9.0
   metadata at the last recorded check.
6. ~~Publish and verify the `hera-godot` npm package `1.0.0`.~~ Done; registry
   installation, release download, checksum verification, and `hera version`
   smoke passed.
7. ~~Update the Homebrew tap and Scoop bucket to `1.0.0`.~~ Done — Homebrew
   commit `3556eb7`; Scoop manifest commit `ca08aeb`; public URLs and checksums
   verified.
8. Recheck the public Godot Asset Store page and record when v1.0.0 and its
   Settings metadata become publicly visible before announcing Store
   availability.
