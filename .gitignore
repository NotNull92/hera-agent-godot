# Go build output
/hera
/hera.exe
/hera-agent-godot
/hera-agent-godot.exe
*.exe
*.test
*.out
/dist/

# Godot generated files
**/.godot/
**/bin/
**/obj/
*.user

# The README-walkthrough scene is assembled live during recording (see
# scenes/demo/README.md); only the committed player.gd is version-controlled.
/scenes/demo/Playground.tscn
/scenes/demo/Playground.tscn.uid

# IDE / OS
.vs/
.idea/
.vscode/
# Local agent workspace state stays per-machine, but the committed Codex
# repo marketplace lives at .agents/plugins/.
.agents/*
!.agents/plugins/
!.agents/plugins/**
# Local Codex state belongs to each machine, not the repository.
.codex/
.debug-journal.md
docs/tests/
.omc/
.omo/
# Claude Code workspace (skills/settings) stays per-machine, not in the repo.
.claude/
.DS_Store
Thumbs.db
