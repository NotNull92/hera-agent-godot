module github.com/NotNull92/hera-agent-godot

go 1.25

// No third-party dependencies yet — the skeleton uses only the standard library.
// A command framework (e.g. cobra) may be added once the command surface stabilizes.
